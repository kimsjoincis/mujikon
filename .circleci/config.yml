version: 2.1

orbs:
  win: 
    circleci/windows@2.2.0
jobs:
  build:  

    machine:
      image: windows-default
    working_directory: ~/repo
    steps:
      - checkout
      - run:
          name: install update
          command: 
            wmic cpu
