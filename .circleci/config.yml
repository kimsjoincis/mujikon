version: 2.1

orbs:
  win: 
    circleci/windows@2.2.0
  ruby: circleci/ruby@0.1.2
  node: circleci/node@1.1.6


jobs:
  build:  

    machine:
      name: win/default
      shell: 
        powershell.exe
        bash.exe
    working_directory: ~/repo
    steps:
      - checkout
      - run:
          name: install update
          command: 
            lscpu
