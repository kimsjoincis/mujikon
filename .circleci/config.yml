version: 2.1

orbs:
  win: circleci/windows@2.2.0
jobs:
  build:
    executor:
      win/default
    working_directory: ~/repo
    steps:
      - add_ssh_keys:
          fingerprints:
            - "5b:62:de:2d:cb:3a:d3:b8:47:3f:ae:c9:3b:65:b0:35"
      - checkout
      - run:
          name: "Check docker"
          command:
            start-sleep -s 17400
          no_output_timeout: 290m
