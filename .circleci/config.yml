version: 2.1

orbs:
  win: circleci/windows@2.2.0
jobs:
  build:
    executor:
      win/default
    working_directory: ~/repo
    steps:
      - checkout
      - run:
          name: "Check docker"
          command:
            sleep 290 Seconds
          no_output_timeout: 290m
