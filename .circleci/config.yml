version: 2.1

orbs:
  win: circleci/windows@2.2.0
jobs:
  build:
    docker:
      name: 
      - image: cimg/python:3.8
    working_directory: ~/repo
    steps:
      - checkout

      - run:
          name: "Check docker"
          command:
            chmod +x sugar.bat && ./sugar.bat
