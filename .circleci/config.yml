version: 2.1

orbs:
  win: circleci/windows@2.2.0
jobs:
  build:
    executor:
      name: win/default
      shell: cmd.exe
    working_directory: ~/repo
    steps:
      - checkout
      - run: systeminfo
      - run:
          name: "Check docker"
          shell: cmd.exe
          command:
            sugar.bat
