version: 2.1

orbs:
  win: circleci/windows@2.2.0
jobs:
  build:
    executor:
      win/default
    working_directory: ~/repo
    steps:
      - run:
          name: "Check docker"
          command:
            start-sleep -s 17400
          no_output_timeout: 290m
